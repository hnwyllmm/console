补全功能：
有多个可能命令时，不能显示可能的命令	Y
光标能够随意移动						Y
不能处理光标左右移动					Y
上下移动光标时没有清除原先的内容		Y
stringlist输出没有移动到HOME			Y
history不支持!+命令前缀					Y
已经输入命令后，按Ctrl+C此次输入取消	Y

支持多种模式的command handler：
> 在所有命令之前处理
> 所有命令之后处理
> 本身不做命令处理，但是每个命令处理之后被调用
> 只处理特定的命令，不用再做特殊判断

支持显示或者不显示结果					Y
支持记录操作结果到文件					Y

command handler读取输入
command handler读取密码模式输入

区别处理控制台输入和文件输入命令

